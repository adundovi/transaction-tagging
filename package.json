{
  "name": "finance-tagging",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "dev": "trunk serve",
    "prod": "trunk build --release && tailwindcss -i index.css -o dist/tailwind.css --minify"
  },
  "devDependencies": {
    "postcss": "^8.4.5",
    "tailwindcss": "^3.0.17"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.2"
  },
  "private": true
}
